# 🛠 Pending Technical Tasks (Unfinished due to Force Stop)

هذا الملف يحتوي على التعديلات البرمجية التي بدأت فيها ولم تكتمل في الـ Backend، وهي ضرورية لكي تعمل ميزة "إعدادات البروفايل المنفصلة" بشكل صحيح.

---

## التاسك: إكمال الربط البرمجي لإعدادات البروفايل (Backend Integration)

### 1. تحديث الـ API Routes (Profiles)
**الملف:** `backend/app/api/routes/profiles.py`
**المطلوب:** التأكد من أن الـ Endpoint الخاص بـ `PUT /packages/{package_id}/profiles/{profile_id}` يستقبل الحقول الجديدة (`max_messages_per_hour`, `max_messages_per_3hours`, `max_messages_per_day`) ويمررها للخدمة (Service).

### 2. معالجة القيم الفارغة (Null Handling)
**الملف:** `backend/app/services/profile_service.py`
**المطلوب:** في دالة `update_profile` يجب التأكد من أن النظام يسمح بإرسال قيمة `null` لهذه الحقول.
- إذا كانت القيمة `number`: يتم تحديث الحد الخاص بالبروفايل.
- إذا كانت القيمة `null`: يتم مسح الحد الخاص بالبروفايل ليعود النظام تلقائياً لاستخدام حدود "الباقة" العامة.

### 3. تحديث استجابة الـ Health Check
**الملف:** `backend/app/services/profile_service.py` (دالة `get_profile_health`)
**المطلوب:** تحديث الـ `limits_usage` لتعرض الحدود "الفعلية" (Effective Limits).
- المعادلة: `effective_limit = profile.limit if profile.limit else package.limit`
- هذا سيجعل الـ UI يعرض الـ Progress Bar بناءً على الرقم الصحيح.

### 4. اختبار استراتيجية التوزيع (Distribution Test)
**المطلوب:** إجراء اختبار إرسال رسائل (Bulk Send) والتأكد من أن الـ `DistributionService` يقوم فعلياً بتوزيع الرسائل بناءً على الحدود الجديدة لكل بروفايل وليس حدود الباقة فقط.

---

**ملاحظة للمطور/الـ AI:**
تم إكمال الـ Frontend بالكامل في `PackageDetail.tsx` والـ Models في `models.py`. التعديلات المتبقية هي فقط في منطق الـ Service والـ Routes لربط الطرفين ببعضهما.
