# Database Configuration
# PostgreSQL (Async for FastAPI)
DATABASE_URL=postgresql+asyncpg://username:password@localhost:5432/block_preventer_bridge
# PostgreSQL (Sync for migrations/scripts)
DATABASE_URL_SYNC=postgresql://username:password@localhost:5432/block_preventer_bridge

# MySQL (alternative - not recommended)
# DATABASE_URL=mysql+aiomysql://username:password@localhost:3306/block_preventer_bridge

# Application Settings
APP_NAME=Whats Guard
APP_VERSION=1.0.0

# IMPORTANT: Do NOT add CORS_ORIGINS or LOG_LEVEL here
# These settings are configured in app/config/settings.py
# Adding them here will cause validation errors

# Optional: Webhook URL for notifications (if needed in future)
# WEBHOOK_URL=https://your-webhook-endpoint.com/notify
